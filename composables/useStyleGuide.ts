export type Variant =
  | "warning"
  | "success"
  | "error"
  | "blue"
  | "grey"
  | "text"
  | "dark"
  | "primary"
  | "secondary";

export type Size = "medium" | "small" | "large" | "x-small";

export const useStyleGuide = () => {
  //const i18n = useI18n();
  //const { setMessage } = useMessageStore();

  return {};
};
